version: "3"
services:
    mtg-notification:
        container_name: mtg-notification
        build: .
        tty: true
        volumes:
            - ./app:/go/src/app
        environment:
            - MIDRA_LAB_NOTION_API=YOUR_API_KEY
            - DISCORD_WEBHOOK_URL=YOUR_WEBHOOK_URL